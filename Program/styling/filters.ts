import { StyleSheet } from "react-native";

export default StyleSheet.create({
    filterTagView: {
        flexDirection: "row",
        alignItems: 'center', 
        backgroundColor:'#FFFFFF', 
        width: 'auto', 
        paddingHorizontal: 5, 
        borderRadius: 5, 
        marginHorizontal: 12
    }
})